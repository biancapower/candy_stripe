<%= form_with(model: purchase, local: true) do |form| %>
  <% if purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :customer_id_id %>
    <%= form.text_field :customer_id_id, id: :purchase_customer_id_id %>
  </div>

  <div class="field">
    <%= form.label :item_id_id %>
    <%= form.text_field :item_id_id, id: :purchase_item_id_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
